<%= form_for(@loot_type) do |f| %>
  <% if @loot_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loot_type.errors.count, "error") %> prohibited this loot_type from being saved:</h2>

      <ul>
      <% @loot_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
    <div class="field">
      <%= f.label :show_on_player_list %><br />
      <%= f.check_box :show_on_player_list %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
