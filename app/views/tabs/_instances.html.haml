%table#instancesTabTable
  %thead
    %tr
      %th Zone
      %th Raid Date
      %th Start time
  %tbody
    - @instance_list.each do |instance|
      %tr
        %td
          - if instance.zone
            = link_to instance.zone.name, instance
          - else
            Unknown
        %td= instance.start_time.to_date
        %td= instance.start_time
%br/